/**
 * This file is partially generated; only edit bespoke sections.
 *
 * SOURCE<<gen/tests.ts::typeTest>>
 * BESPOKE<<imports, beforeAll, afterAll, beforeEach, afterEach, type1, type2, type3, type4, type5>>
 * SIGNED<<Za0Ygv/svgcAOwXmgh4qcWYTAZClbfpuUcyY2SAFiS7cVEMAzYo48eQOpxQWtMVV3+AWu6rUF1XE+9dgb2YRfQ==>>
 */

/* BESPOKE START <<imports>> */
import { type1, type2, type3, type4, type5 } from "./examples";
/* BESPOKE END <<imports>> */

beforeAll(
  async (): Promise<void> => {
    /* BESPOKE START <<beforeAll>> */
    /* BESPOKE END <<beforeAll>> */
  },
);

afterAll(
  async (): Promise<void> => {
    /* BESPOKE START <<afterAll>> */
    /* BESPOKE END <<afterAll>> */
  },
);

beforeEach(
  async (): Promise<void> => {
    /* BESPOKE START <<beforeEach>> */
    /* BESPOKE END <<beforeEach>> */
  },
);

afterEach(
  async (): Promise<void> => {
    /* BESPOKE START <<afterEach>> */
    /* BESPOKE END <<afterEach>> */
  },
);

test(
  "type1",
  async (): Promise<void> => {
    /* BESPOKE START <<type1>> */
    expect(type1.print({
      name: "type1",
      path: "src/__tests__/type.test.ts",
    }))
      .toMatchSnapshot();
    /* BESPOKE END <<type1>> */
  },
);

test(
  "type2",
  async (): Promise<void> => {
    /* BESPOKE START <<type2>> */
    expect(type2.print({
      name: "type2",
      path: "src/__tests__/type.test.ts",
    }))
      .toMatchSnapshot();
    /* BESPOKE END <<type2>> */
  },
);

test(
  "type3",
  async (): Promise<void> => {
    /* BESPOKE START <<type3>> */
    expect(type3.print({
      name: "type3",
      path: "src/__tests__/type.test.ts",
    }))
      .toMatchSnapshot();
    /* BESPOKE END <<type3>> */
  },
);

test(
  "type4",
  async (): Promise<void> => {
    /* BESPOKE START <<type4>> */
    expect(type4.print({
      name: "type4",
      path: "src/__tests__/type.test.ts",
    }))
      .toMatchSnapshot();
    /* BESPOKE END <<type4>> */
  },
);

test(
  "type5",
  async (): Promise<void> => {
    /* BESPOKE START <<type5>> */
    expect(type5.print({
      name: "type5",
      path: "src/__tests__/type.test.ts",
    }))
      .toMatchSnapshot();
    /* BESPOKE END <<type5>> */
  },
);
